# Core training
torch>=2.3.0
transformers>=4.44.0
tokenizers>=0.20.0
datasets>=2.20.0
accelerate>=0.33.0
safetensors>=0.4.3
numpy>=1.26.0
einops>=0.8.0

# Evaluation (official benchmark harness)
lm_eval>=0.4.5,<1

# Logging / tracking
wandb>=0.17.0

# Config, CLI, tests
PyYAML>=6.0.1
tqdm>=4.66.0
pytest>=8.0.0